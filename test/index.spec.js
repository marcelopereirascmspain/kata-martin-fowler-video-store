import expect from 'expect';
import module from '../src';

describe('...', () => {
  it('should...', () => {
    let actual = true;
    let expected = false;

    expect(actual).toEqual(expected);
  });
});
